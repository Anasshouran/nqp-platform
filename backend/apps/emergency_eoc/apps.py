from django.apps import AppConfig

class EmergencyEocConfig(AppConfig):
    default_auto_field = 'django.db.models.BigAutoField'
    name = 'apps.emergency_eoc'
    verbose_name = 'غرفة الطوارئ (EOC)'
